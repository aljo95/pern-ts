{
  "description": "root-concurrently",
  "scripts": {
    "build": "npx tsc",
    "start": "concurrently \"cd react-client && npm run dev\" \"cd express-server && npm run build && npm run start\" "
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
